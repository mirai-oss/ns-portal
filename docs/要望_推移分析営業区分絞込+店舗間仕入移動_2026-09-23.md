# ユーザー要望2件: ①推移分析の営業区分絞り込み ②店舗間の仕入れ移動機能

**発行**: 担当D（勤怠・給与・監視）2026-09-23セッション。ユーザーから直接依頼（「担当違うかも
しれないけど」との前置きあり）。両方とも`tori-dashboard`（app.js・PL管理／推移分析タブ）の
UIとロジックに関わるため、担当A（tori-dashboard/経営ダッシュボード管轄）へ引き継ぐ。担当Dの方で
調査・設計の下ごしらえまで行った。**担当Aは現在、秋葉原肉寿司の列重複バグ復旧＋前回引き継いだ
①変動経費PL未反映調査／②精算書PL反映ボタン化を優先中のため、今回の2件はその後に着手でよい**
（緊急ではない）。

---

## ①推移分析タブで「営業区分」（店内売上/ランチ/ディナー/デリバリー）の絞り込み

### ユーザー要望（原文）

> 推移分析で営業区分で絞り込みできるようにしてほしい。（店内売上（ランチ＋ディナー）、ランチ、
> ディナー、デリバリー）

### 調査結果

- `tori-dashboard/app.js`の`viewAnalysis()`（3256行目〜、推移分析タブ本体）には**営業区分の
  絞り込みが現状一切無い**（確認済み）。
- 似た絞り込みUIは`viewDetail()`（明細分析タブ・3549行目〜）に既にある（`S.dSegment`・
  「営業区分：全体／ランチ／ディナー」の3択、3580〜3584行目）。ただし**デリバリーの選択肢は無い**
  し、こちらは明細分析タブ専用で推移分析とは別のデータ経路・別の絞り込みstate（`dSegment`）。
- 明細分析タブのランチ/ディナー判定は「**実データではなく推定**」（3587行目のnote-boxで明記）:
  fact_daily_storeが日別合計しか持たずランチ/ディナー別を持たないため、POS明細(dinii)から算出した
  「その区分の構成比」でレジ実績を按分している。境目は店舗×曜日ごとの予約タブ「営業時間」設定
  （昼の部閉店時刻。未設定は16:00既定）で判定。
- 推移分析タブの主データ源`kd_dashboard_daily_summary`（`keiei-kd-refresh/index.ts`の
  `refreshDashboardDaily`で確認済み）は`net_sales/guests/parties/cost/labor/labor_pa/labor_emp`の
  列**のみ**で、ランチ/ディナー/デリバリーの内訳列は無い。

### 実現の難易度が2つに分かれる

- **デリバリー**: 比較的容易。ロケットナウ等の売上は`delivery_store_map`経由で実データとして
  BigQueryに投入されており（9/17に月次売上への合算機能を実装済み）、「推定」ではなく実額。
  `kd_dashboard_daily_summary`に`net_sales_delivery`のような列を1本追加し、店内売上と分けて
  持たせれば、推移分析側は既存の日次集計の仕組みにそのまま乗せられるはず。
- **ランチ／ディナー**: 難しい。明細分析タブと同じ「POS明細からの構成比按分」を使うとすれば、
  推移分析は数ヶ月〜数年単位の長期トレンドを見るタブのため、**年単位のレンジで按分計算を
  行うと重くなる可能性がある**（明細分析タブは基本「日/週/月/年」の単一期間だけを都度計算する
  設計）。長期間を通しで見せるには、日次バッチ（`refreshDashboardDaily`と同じタイミング）で
  按分計算を行い`kd_dashboard_daily_summary`へランチ/ディナー別の列として事前計算・保存して
  おく方式が現実的と思われるが、実データ量・BQクエリコストの見立てはこちらからは確認できない
  （Supabase/BQへの接続情報がこのMacBookに無いため）。

### 担当Dからの提案

1. まず**デリバリーの絞り込みだけ先に実装**（実データがあり技術的難易度が低い）。
2. ランチ／ディナーは、日次バッチで事前集計して保存する設計に倒せそうか担当Aに技術検証しても
   らってから着手（「店内売上（ランチ＋ディナー）」は①ランチ列＋ディナー列の合算、または
   ②単純に「デリバリー以外」として`net_sales - net_sales_delivery`で計算、のどちらでも出せる）。
3. 按分方式を使う以上、明細分析タブと同じ「推定である」旨の注記をUIに出すこと（既存パターン踏襲）。

---

## ②店舗間の仕入れ移動機能

### ユーザー要望（原文）

> 店舗間移動の仕入れがあった場合に、PL管理、もしくは経営ダッシュボードのところから仕入れ、移動等
> のボタンを作って、店舗間の仕入れ金額を移動させる機能を作って欲しい（例として本店→芝で9月1日
> 10,000円サムゲタン、をやったら、本店の分が仕入れから10,000円差し引かれて芝に10,000円プラスされる
> ような形にしたい）※もちろん原価率はその店舗間移動された金額が反映されるように

### 担当Dが用意したもの

**新規テーブルの提案SQL**（未実行・未レビュー）: [supabase/2026-09-23_store_cost_transfers.sql](../supabase/2026-09-23_store_cost_transfers.sql)

- 1回の移動＝1行（移動日・移動元店舗・移動先店舗・品名・金額・任意メモ）。
- 取り消しは行削除ではなく`canceled_at`を立てるソフトデリート方式（店舗間のお金の動きは監査性を
  優先。DB_PL側の他の仕組みは削除方式だが、こちらは履歴を残す設計を提案）。
- 書き込み権限はまだ絞っていない（service_role経由のみを想定。実装方針が決まってからRLSを追加）。

### 集計ロジックへの組み込み方（提案・実装は担当Aへ）

`ns-portal/supabase/functions/keiei-kd-refresh/index.ts`の`refreshPlMonthly()`
（PL集計の月次リフレッシュ本体。452行目〜）は、店舗×年月ごとに`cost_manual`等を積み上げる
`byKey`というバケット構造を使っている。この関数は**担当Aが現在進行系で頻繁に触っている**ため、
ここでは正確な行番号diffではなく方針だけ示す（実装時に担当Aの最新版へ当ててほしい）:

1. `refreshPlMonthly`の対象年月が決まった後、その年月の`store_cost_transfers`
   （`canceled_at is null`のもの）を取得する。
2. 各行について:
   - `from_store_id`のバケットの`cost_manual`から`amount`を**減算**（＋`pl_item_breakdown.F`に
     `店舗間移動：<item_name>（→<to_store名>）` = `-amount`として内訳表示）
   - `to_store_id`のバケットの`cost_manual`に`amount`を**加算**（＋`pl_item_breakdown.F`に
     `店舗間移動：<item_name>（←<from_store名>）` = `+amount`として内訳表示）
3. 既存の`cost_total = costAuto + cost_manual`のロジックはそのまま使えるので、原価率（F%＝
   cost_total/sales）は自動的に移動後の金額で計算される（要望の「原価率も反映」を満たす）。
4. `sync_run_id`との整合や、このリフレッシュがstore_id単位のバケットを都度作り直す設計と
   `store_cost_transfers`が独立テーブルである点から、二重計上・消し忘れの心配は無い（毎回
   `store_cost_transfers`から数え直すだけなので、`syncSeisanCategoriesToPl`のような
   「自分の書いた行をクリーンアップしてから再構築」という複雑な仕組みは不要）。

### UI（tori-dashboard app.js側・担当Aへ）

- `viewPL()`（6046行目〜、PL管理タブ）に「仕入れ移動」ボタンを追加し、モーダルで
  日付・移動元店舗・移動先店舗・品名・金額を入力→登録。
- 一覧（直近の移動履歴）と、行ごとの「取り消し」ボタンも同画面に置くのが自然（`canceled_at`を
  立てるだけのシンプルな操作）。
- 登録・取り消しのAPIは新しいEdge Function、または既存の`keiei-kd-refresh`系Edge Functionに
  アクションを1つ追加する形が良さそうだが、既存のAPI設計パターン（`pl-fee-reflect`等）に
  合わせるのが良いか、担当Aの判断に委ねる。
- 登録・取り消し操作の直後に、その年月の`refreshPlMonthly`を再実行してPL管理タブへ即座に反映
  されるようにする（前回引き継いだ「精算書PLに反映ボタン」の要件＝即時反映、と同じ考え方）。

---

## 参考

- `tori-dashboard/app.js` 3256行目〜`viewAnalysis()`（推移分析）
- `tori-dashboard/app.js` 3549行目〜`viewDetail()`（明細分析・既存の営業区分絞り込みの参考実装）
- `tori-dashboard/app.js` 6046行目〜`viewPL()`（PL管理）
- `ns-portal/supabase/functions/keiei-kd-refresh/index.ts` 452行目〜`refreshPlMonthly()`
- [supabase/2026-09-23_store_cost_transfers.sql](../supabase/2026-09-23_store_cost_transfers.sql)（今回追加した提案テーブル。未実行）

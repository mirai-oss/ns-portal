# 設計書: PL補助科目・スポット人件費・マネーフォワード試算表CSV取込

作成: 2026-08-23 ／ ユーザーヒアリング済み（4論点確定）／ 実装は未着手
対象読者: 実装スレッド。現行構造は `docs/売上ダッシュボード_データ設計仕様書_2026-08-23.md` §2 を先に読むこと。

**確定した前提（2026-08-23 ユーザー回答）**:
①スポット人件費は**日付単位**で入力（日別の人件費率・FL率に反映）
②マネーフォワード会計は**部門=店舗**で試算表を出せる（店舗別取込が可能）
③CSV取込で金額不一致の場合は**プレビューで1件ずつ「残す/上書き」を選ばせる**
④補助科目は**マスタ管理＋入力時のその場追加**

## 0. 現行データモデル（変更対象）

- `DB_PL`シート: 年月／店舗名／勘定科目／区分(S,F,L,R,A,O,X)／金額／メモ。店舗名空欄=全社共通
- 書込経路: ダッシュボードPLタブ（`savePlEntries`/`savePlBulk`）＋PL管理システム`✍販管費入力`（毎日8:00と10分おきの自動同期）＋自動行（`自動｜媒体販促費`・`自動｜法定福利費`・`自動｜通勤手当`・運営委託費）
- BQミラー: `sales.stg_pl`（PL書込時に`bqSyncPL`が自動追随・speed-v6で修正済み）
- 人件費: L01社員/L02PA は`分析_日別店舗`由来の自動値（DB_PLには入れない）

---

## 1. 補助科目（勘定科目の下の階層）

### データモデル（すべて追加のみ・既存を壊さない）

| 変更 | 内容 |
|---|---|
| `DB_PL`に7列目「補助科目」追加 | 既存行は空欄=補助科目なし。一意キーは**年月×店舗×勘定科目×補助科目** |
| 新シート`DB_補助科目`（マスタ） | 列: 勘定科目／補助科目／表示順／有効。GASが雛形自動生成（既存DB_系と同方式）。`getData`で配信 |
| `stg_pl`に`sub_item`列追加 | `bqSyncPL`・`bqGetPL`のSQLに列追加 |
| PL管理システム`✍販管費入力`にG列「補助科目」追加 | 自動同期（pl_dbpl_autosync）の転記範囲をG列まで拡張 |

※科目マスタ（60科目・行番号固定）には触らない。補助科目は独立マスタ。

### UI

- **入力**: 勘定科目を選ぶと補助科目プルダウンが出る（マスタ由来＋「＋新しい補助科目を追加」でその場登録→マスタへ保存）。空欄（補助科目なし）も可
- **PL表示**: 勘定科目行は**合計値**を表示し、行頭の▶/▼で補助科目内訳を開閉（既定=閉じた状態・インデント表示）。「すべて展開/すべて折りたたむ」ボタンをタブ上部に
- **集計は勘定科目合計ベースのまま**（FL率・営業利益等の既存計算ロジックは不変。補助科目は表示上の内訳のみ）→ 数値のリグレッションリスクを構造的にゼロにする

## 2. スポット人件費（タイミー等の単発人件費）

### データモデル

- 新シート`DB_スポット人件費`: 日付／店舗名／区分（タイミー/その他 ※プルダウン・マスタは初期2種で開始）／金額／人数(任意)／メモ／入力者／入力日時
- BQミラー: `bqSyncSales`の対象に`stg_spot`を追加（毎朝08:45/11:00の既存同期に乗る）

### 反映（2系統）

| 反映先 | 方式 |
|---|---|
| **日別の人件費率・FL率**（ダッシュボード） | `getData`/`bqDailyStore`にspotを配信し、**フロント（app.jsのstat()）で日別加算**。人件費合計=PA＋社員＋スポット。内訳表示に「スポット」行を追加。※`分析_日別店舗`・`fact_daily_store`の生成ロジックには一切触らない（安全第一） |
| **月次PL** | 既存の「自動｜」行パターンを踏襲: 月次集計を`自動｜スポット人件費`（L区分）としてDB_PLへ日次upsert（morning-refreshに1ステップ追加）。手入力と混ざらず、二重計上しない |
| Lark日報（reportDataBQ） | 人件費集計に`stg_spot`を加算（1クエリ追加） |

### UI・権限

- 入力導線: PLタブと日別ダッシュボードの人件費カードに「＋スポット人件費」ボタン → 日付・店舗・区分・金額・メモ（3タップ級）
- 一覧・修正・削除: 同月内は編集可。一覧に入力者と入力日時を表示
- 権限: 既存の「使える機能」方式に`spot`キーを追加（既定: 本部・社長=全店／店長=自店舗）

## 3. マネーフォワード試算表CSV取込

### フロー

```
MF会計で部門別試算表CSVを出力（法人ごと・月次）
 → ダッシュボード「📥 MF取込」画面にアップロード
 → パース＆照合 → プレビュー（登録/除外/要選択/未対応の4分類）
 → ユーザーが確定 → DB_PLへ書込（bqSyncPL自動追随）
```

### 照合ロジック

1. **部門名→店舗名**: Supabase`store_aliases`に部門名を別名として登録して解決。未解決部門はプレビューで店舗を選択→aliasに保存（次回から自動）
2. **科目名の対応**: 新シート`DB_科目対応`（MF勘定科目／MF補助科目 → 内部勘定科目／補助科目／区分／**取込対象外フラグ**）。未対応科目はプレビューで対応付け→保存で学習
3. **判定**（キー=年月×店舗×勘定科目×補助科目）:
   - DB_PLに無い → 「**登録**」
   - あり・金額一致 → 「**除外**（取込済み）」
   - あり・金額不一致 → 「**⚠️要選択**」: プレビューで1件ずつ「残す/上書き」（一括ボタン「すべて残す」「すべて上書き」も用意）
4. **二重計上ガード（最重要）**: 以下は既に自動連携済みのため**既定で取込対象外**（`DB_科目対応`のフラグで管理・画面から変更可）:
   - 人件費系（給与手当・雑給・法定福利費・通勤手当）→ 分析_日別店舗/自動行由来
   - 運営委託費 → 精算書から毎週自動連携済み
   - 売上・仕入系 → 日次取込由来
   - 経費申請と重複しうる科目は除外はしないが、同月×同店舗×同科目に経費申請由来の登録がある場合プレビューに「⚠️経費申請と重複の可能性」を表示
5. 取込で登録した行はメモ欄に`[MF取込 2026-08-23]`を自動記載（手入力との区別・次回判定の材料）

### プレビュー画面

サマリ（登録N件／除外M件／要選択K件／未対応科目J件）＋明細テーブル。**要選択と未対応が0になるまで確定ボタンは押せない**。確定後に結果（登録件数・BQ同期結果）を表示。

### 実CSVサンプルで判明した仕様の補正（2026-08-23、鳥一代本店の実物で確認・ユーザー回答済み）

1. **CSVは「月次推移」形式（1年12ヶ月分が横並び）で毎回出力される**。単月出力ではなく、**アップロード1回で12ヶ月ぶん（4月〜3月列）をまとめてパース→照合**する。金額・科目が既存DB_PLと完全一致する月は自動的に「除外」判定（既存の照合ロジックそのまま）になるため、実質「差分だけ登録」される。実装上の変更点: パーサは月ごとの列をループしてキー（年月×店舗×勘定科目×補助科目）を月の数だけ生成する。
2. **「補助科目」列に3種類の値が混在する**（勘定科目によって中身が違う）:
   - 本当の補助科目（例: 水道光熱費→電気料金/ガス料金/水道料金）→ そのまま取り込む
   - 取引先名（例: 仕入高→タジマフーズ等、広告宣伝費→食べログ等）→ **無視して勘定科目合計に合算**（取引先名を補助科目マスタに登録しない）
   - 部門コード（例: 「01/本店」「01/本店/鳥一代」。売上高・給料手当・通勤手当などで出現）→ **無視して勘定科目合計に合算**
   - 判定方法は実装時に検討: 値が「数字/」で始まる、または`DB_科目対応`にも`DB_補助科目`マスタにも一致しない場合は「取引先名または部門コード」とみなし合算する、といったヒューリスティックが必要（要実装時の詳細設計）。もしくは`DB_補助科目`マスタに事前登録された値のみ補助科目として採用し、それ以外はすべて合算する方式が単純で安全。
3. これにより§0の判定ロジック（登録/除外/要選択/未対応）は**月ごと・キーごとに独立して判定**する（12ヶ月分＝最大12件の判定結果が1科目から生まれうる）。

## 4. 実装順と目安

| 順 | 内容 | 目安 | 依存 | 状態 |
|---|---|---|---|---|
| 1 | 補助科目（列追加・マスタ・開閉UI・BQ列） | 1日 | なし（他2つの土台） | ✅ コード実装済み（2026-08-23・ローカル）。**未デプロイ**。下記「デプロイ手順」参照 |
| 2 | スポット人件費（シート・入力UI・日別加算・自動行・BQ） | 1日 | なし | ✅ コード実装済み（2026-08-23・ローカル）。**未デプロイ**。下記「デプロイ手順」参照 |
| 3 | MF取込（パーサ・対応表・プレビュー・確定処理） | 2〜3日 | 1（補助科目キーが前提） | 未着手（CSVサンプル1枚入手済み・照合ルールの補正は上記反映済み） |

### ①補助科目のデプロイ手順（2026-08-23実装・次にやる人向け）

変更したファイル: `tori-dashboard`リポジトリの `app.js` / `gas/Code.gs` / `gas/pl-system/pl_dbpl_autosync.gs` / `gas/pl-system/pl_system.gs`。
この環境はclasp未接続（手動でApps Scriptエディタにコピペする運用）。手順:

1. **ダッシュボード側**（[スプレッドシート](https://docs.google.com/spreadsheets/d/1OuaAQBeXHxJZtDXEbQx-V7w56fCWW5jpDmZvBpkfIbQ/edit) → 拡張機能 → Apps Script）:
   - `Code.gs`の内容をリポジトリの`gas/Code.gs`で上書き
   - webアプリを「新バージョンとして」再デプロイ（`?action=ping`で`ver`が更新されたことを確認）
   - `app.js`をindex.html配信元（GitHub Pages等、既存の配信手順どおり）に反映
   - 初回アクセス時に`DB_PL`のG列（補助科目）と`DB_補助科目`シートが自動生成される（`setupIfNeeded`内、既存データは一切変更しない）
2. **PL管理システム側**（[スプレッドシート](https://docs.google.com/spreadsheets/d/1ZJ5a3ZgsRGfJHVhIXo2b-OK-2gZMvUAHl7J9WFms7dQ/edit) → 拡張機能 → Apps Script。ダッシュボードとは別プロジェクト）:
   - `pl_dbpl_autosync.gs`ファイルを上書き（新規なら追加）
   - `pl_system.gs`は**そのまま上書きしない**こと（本番の`コード.gs`は`syncAd`等リポジトリに無い関数を含む可能性があるとコメントにあるため）。今回の変更点（`buildInput`のG列対応・`syncToDbPl`/`autoPromoToDbPl_`/`readDbPl`の7列対応）は、本番の`コード.gs`側に**該当関数だけを見比べて手動反映**する
   - 反映後、Apps Scriptエディタで`plsMigrateAddSubItemColumn_`を選んで実行 → 「✍ 販管費入力」にG列（補助科目）が安全に追加される（既存データは無変更）
3. 動作確認: ダッシュボードのPLタブで経費入力モーダルを開き、補助科目を入力→保存→表の勘定科目行の▶をクリックして内訳が出るか確認

### ②スポット人件費のデプロイ手順（2026-08-23実装・次にやる人向け）

変更したファイル: `tori-dashboard`リポジトリの `app.js` / `gas/Code.gs` ＋ 新規 `.github/workflows/spot-labor-pl-sync.yml`。
①と同じくダッシュボード側Apps Scriptエディタへの反映が必要（PL管理システム側は今回変更なし）。手順:

1. [ダッシュボードのスプレッドシート](https://docs.google.com/spreadsheets/d/1OuaAQBeXHxJZtDXEbQx-V7w56fCWW5jpDmZvBpkfIbQ/edit) → 拡張機能 → Apps Script で`Code.gs`を上書き→新バージョンとして再デプロイ（`?action=ping`で`ver`更新を確認）
2. `app.js`を配信元に反映
3. 初回アクセス時に`DB_スポット人件費`シートが自動生成される（既存シートには影響なし）
4. `.github/workflows/spot-labor-pl-sync.yml`をpush（毎日JST5:00に`syncSpotLaborToPl`を自動実行。`seisan-fee-sync.yml`と同じ`BQ_LOAD_TOKEN`シークレットを使うのでリポジトリ側の追加設定は不要のはず）
5. 動作確認: 推移分析タブ（またはダッシュボード）のFL内訳パネルで「＋ スポット人件費」から1件保存→人件費率に反映されるか／PLタブに翌日以降「スポット人件費」科目が出るか確認

### ②の既知の未対応範囲（次にやる人向け・意図的にスコープ外にした）

- **AI検索タブ**の財務回答（app.jsの`D.daily`を直接集計している別ロジック、4890行目付近）は今回**スポット人件費を合算していない**。ダッシュボード本体（推移分析・PLタブ）とAI検索で人件費の数字が食い違う可能性があるので、対応するなら`D.spot`を同様に合算する修正が必要
- 補助科目と同様、スポット人件費も**取込CSVの実物やタイミー等の運用実態は未確認**。区分の初期値は設計どおり「タイミー／その他」の2種で実装したが、実際の入力導線で運用に合うか要確認

## 5. 実装時にユーザーへ確認・依頼すること

1. **MF試算表CSVのサンプルを法人ごとに1枚ずつ**（列構成の実物確認。部門別出力の設定状態も）
2. 補助科目の初期マスタの種（例: 水道光熱費=電気/ガス/水道、通信費=電話/ネット 等。最初に入れておく分）
3. MF部門名と店舗名の対応の初期確認（初回取込プレビューで一緒に確定すればよい）
4. スポット区分の初期値（タイミー/その他 で開始してよいか）

## 6. 壊さないための検証項目

- [ ] 既存PL表示の数値が変わらない（補助科目導入後、勘定科目合計=導入前の値）
- [ ] `✍販管費入力`→自動同期がG列込みで動く（既存行・補助科目空欄行も従来どおり）
- [ ] スポット追加後の日別人件費率が「PA＋社員＋スポット」で再計算され、Lark日報とも一致
- [ ] MF取込を同じCSVで2回実行すると、2回目は全件「除外」になる（冪等性）
- [ ] `bqReconcileSales`の突合が引き続き完全一致（スポットは別テーブルのため影響しない設計だが確認）

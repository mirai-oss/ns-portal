# このリポジトリで作業するときの必須ルール

このプロジェクトは複数のPC・複数のスレッド（セッション）が入れ替わり立ち替わり作業する。**`WORKLOG.md`が全体の唯一の正**（要件定義書の最新版・システム全体像・DBテーブルの持ち主・接続情報・進行中の作業まで、冒頭を読めば分かるようになっている）。このルールを守らないと、次にどのPC・どのスレッドが来ても現状が分からなくなる。

## 作業を始める前に必ずやること

1. `WORKLOG.md`の冒頭「📍現在の状況」を読む。
2. `git fetch origin && git log --oneline -1 origin/main` で実際の最新コミットを確認する（WORKLOGに書いてあるハッシュは更新時点のスナップショットなので鵜呑みにしない）。ローカルより進んでいたら `git pull --rebase origin main`。
3. `nippo`リポジトリも同じSupabaseプロジェクト（ハブ）を共有している。nippoを触る／nippo側の状態を前提にする場合は、そちらでも同様に`git fetch`すること。

## 作業を終える前に必ずやること（絶対に省略しない・「あとでまとめて」にしない）

1. 変更をコミットし、`git push`する。
2. `WORKLOG.md`に今回の作業内容を時系列セクションとして追記する（日付見出し＋要点。詳細な引継ぎ書を別途作った場合もここに一行リンクを残す）。
3. `WORKLOG.md`冒頭の **「📍現在の状況」を今の状態に書き換える**（上書き。追記ではない）:
   - 最終更新日時
   - 要件定義書の現在バージョン（版が上がっていれば更新）
   - 進行中の作業・直近やったこと
   - `ns-portal`・`nippo`それぞれの直近コミットハッシュ
   - 次にやること候補
4. 更新した`WORKLOG.md`をコミットし、`git push`する。

この4ステップを終えて初めて「作業完了」。押し忘れ・書き換え忘れが一番の事故原因になる。

## トークン（クレジット）節約の鉄則

- WORKLOGは**冒頭の「📍現在の状況」〜索引まで**だけ読む（時系列ログの全文読みはしない。過去の経緯が必要なときだけ該当日付を検索して部分読み）。
- 要件定義書・設計書は**作業に関係するセクションだけ**読む（索引→該当§へ）。
- WORKLOGの時系列ログが肥大化したら（目安1,500行超）、古いエントリを`WORKLOG_ARCHIVE.md`へ移して本体を軽く保つ（📍と索引は常に本体に残す）。

## その他の鉄則（詳細は`WORKLOG.md`参照）

- 本番Supabaseへの SQL実行・Edge Functionのデプロイはユーザー確認を得てから行う。
- 既存のマイグレーションSQLファイルは編集しない（新しい変更は日付入りの新規ファイルを追加）。
- 既存の本番Edge Function（`line-webhook`・`smaregi-sync`・`smaregi-shift-sync`等）を書き換えるときは、本番バイナリから完全なソースを復元しdiffで既存部分が無変更であることを確認してからデプロイする。
- 本番データを直接テストに使わない。テストは使い捨てユーザー・使い捨てデータで行い、作業後に完全削除して0件を確認する。

# 実装指示書: 担当C — 請求書メール管理 Phase 1.5

作成: 2026-08-24 ／ あなたは並行開発の担当Cです。**まず`docs/実装指示書_並行開発の担当分け_2026-08-24.md`の「共通の鉄則」と「所有権マトリクス」を読むこと**。

## あなたの縄張り

- `ns-portal/invoices.html`／`supabase/2026-08-23_invoices.sql`（**この機能は例外的に1ファイル追記運用**）／`invoice-*`Edge Function／GAS `gas/invoice-intake/`（請求書用の独立プロジェクト。**他のGASには触らない**）
- ポータル`index.html`はタイル・バッジの小変更のみ可（WORKLOGで宣言してから）

## 前提知識（着手前に読む）

1. `ns-portal/WORKLOG.md`冒頭 → 2. **`docs/引継ぎ書_2026-08-24_請求書メール管理Phase1完了.md`**（これが正。システム構成・できること・確定済み設計判断・実機で踏んだ罠がすべて載っている。元の実装指示書は読まなくてよい）

**変更してはいけない確定済み設計判断**（引継ぎ書§3。変えたい場合は必ずユーザー確認）: 取込対象=info@宛全メール＋「請求書」ラベル／社長個人宛Toは除外／差出人はinfo@固定／「対象外」と「取込除外ルール」は別物／重要フラグは処理状態と独立。

## C-1: OCR実機確認（ユーザーのAPIキー待ち）

AI自動読取（Anthropic API・実装/デプロイ済み）はAPIキー登録待ち。キーが届いたら: 実メール数十件で読取精度を確認→請求元・金額・期限の抽出ミスのパターンを収集→プロンプト調整。**APIキーはSupabaseのEdge Function環境変数のみに保存**（コード・リポジトリ・GAS禁止）。

## C-2: Phase 1.5 機能強化

1. **検索・絞り込み**: 件名・送信元・本文のフリーワード検索＋期間・金額範囲・状態の複合絞り込み（一覧上部に検索バー。DBはinvoice_emailsへのindex追加=SQL追記でOK）
2. **金額・支払期限のソート表示**: 一覧の列ヘッダクリックでソート。**期限が近い/超過を赤系バッジで強調**（プロトタイプの配色ルール: 赤=期限・重要）
3. **コメント機能**: メール単位のコメントスレッド（`invoice_comments`テーブルをinvoices.sqlに追記・RPC経由書込・監査ログ記録・処理担当者の判断メモ用途）
4. 実運用フィードバックが届いたら上記より優先して対応（ユーザーが毎日使っている機能のため）

## C-3（余力あれば）: 労務ワークフローの設計書ドラフト

労働条件通知書の「本部が時給等入力→送信ボタン→自動生成→本人送付→`info.documents`保管」フロー（G3）。**実装はしない・設計書のみ**。送信基盤が請求書のGAS送信経路と同型のためCが設計適任。着手前に`実装指示書_勤怠月またぎ再取込と新規要望4件_2026-08-24.md`タスク2の確認事項（テンプレートの雛形・送信経路メール/LINE・電子交付同意の要否）をユーザーに提示して回答を得ること。

## C-4（依頼が来たら）: embed対応

担当F（ポータルシェル）から「`?embed=1`のときinvoices.htmlの自前ヘッダー/ナビを隠す」依頼がWORKLOGに来たら対応（1〜2時間）。

## 注意・ゲート

- 本番SQL・Edge Functionデプロイ・**メール送信を伴うテスト**は都度ユーザー確認。送信テストは**必ず自分宛メール**で（取引先に絶対送らない=Phase 1からの鉄則）
- 引継ぎ書§4の実機の罠（`deliveredto:`は使わない・Storageキーに日本語不可・upsert統一・limitとKPIのズレ・pushState）を再発させない
- 実データ（本物の請求書メール）は消さない・書き換えない。テストは自分宛メールで作ったデータで行い削除まで

## 完了条件

- [ ] OCRが実運用精度でフォーム提案し、ユーザーが「使える」と判断
- [ ] 検索・ソート・コメントが動き、スマホでも操作できる
- [ ] 既存のPhase 1全機能（取込・ロック・返信・除外ルール等）が壊れていない（回帰確認）
- [ ] 各区切りでWORKLOG 4ステップ

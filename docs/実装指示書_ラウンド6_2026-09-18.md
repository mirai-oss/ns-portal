# 実装指示書: ラウンド6（2026-09-18）— 最優先=経営ダッシュボードの安定（ログイン根治）と速度・次点=精算書

発行: 司令塔 ／ ユーザー指示「全担当の進行/停止を整理し担当ごとに指示書を。最優先=経営ダッシュボードのスピード安定とログイン安定。次=精算書のスピード。BQで止まることが多いので別の組み立てで。特にPL管理が激遅」

## 0. 全レーン再開表（2026-09-18・司令塔実査）

| レーン | 直近の実績（主なもの） | 状態と次 |
|---|---|---|
| A | TK-180精算GAS調査（一過性不調と判断）・ロケットナウ精算差異修正・権限一元管理調査（TK-39進行中） | **§1の最優先パッケージへ全振り**（A-11/A-12/A-13）。TK-39は中断可 |
| B | シフトUIv12刷新＋給料振込/現金手渡しの本部タスク自動発行（対象者混入まで修正済み） | 継続（現行作業を完了させる）。B-16/17（有給）は後段 |
| C | 会計WSをv14まで高速進化（売上入金・仕訳自動化・PL反映導線）・**請求書一覧の遅さ=iframe毎回作り直しが本命と特定し修正済み**（ユーザー実機確認待ち） | TK-73（請求書フロー整理）・TK-85（業務委託精算C側）継続 |
| D | **ログイン不安定の対症療法**（404/405リトライ・v196）・**ポータル→経営Dの自動ログイン統合**（全役職・本番反映済み）・デリバリー売上合算 | D-9継続・TK-64/66・ロケットナウPhase2（Playwright）。§1でA支援 |
| P | kd_サマリ群稼働・perflog運用 | **TK-63（kd_store_monthly）＋§1支援（kd_pl毎時化・sessionsテーブル）** |
| E | 本部タスク種別タブ・入社タスク対応済み | 待機（会計例外連携はC進行に合わせ対応） |
| F | 承認待ちバッジ等 | **TK-34（緊急: 個人情報変更申請が誰にも使えないバグ）を最優先**→TK-69/70/71 |
| G/H | 待機／台帳運用 | H=本ラウンドのタスク反映 |

## 1. 🥇最優先パッケージ「経営ダッシュボードの安定と速度」（担当A主担当・P/D支援）

**背景（確定済みの事実）**: ログイン不安定の真因は①GAS Webアプリの一時不調（Googleボット判定で404/405が15〜30秒続く→従来リトライ対象外→unauthorized誤判定→強制ログアウト。9/17に対症療法=リトライ拡大済み）②**構造的リスク=セッションがGASのPropertiesService（単一共有ストア）保存**（利用者増で競合・容量の不安定要因。9/17に担当Dが「Supabaseへ移す設計変更を推奨・ユーザー確認待ち」と記録）。**本ラウンドでユーザー最優先指示を受け、②の実施を司令塔が承認扱いとする**。

### A-11: セッションのSupabase一本化（ログイン根治・最優先。**2026-09-18ユーザーフィードバックでスコープ強化**）
**ユーザー体感（9/18）**: 「そもそもログインして開けないことがある」「精算書もログイン後に勝手にログアウトされる」→ ①**ログイン処理そのもの**（`supalogin`）と初回ロードが不調時間帯（404/405が15〜30秒）に当たると開けない ②**精算ダッシュボードも同じPropertiesServiceセッション構造**で同症状。以下に拡大:
1. **共通セッションへ一本化**: セッション正本をハブSupabaseの専用テーブル（`ds_sessions`等・Pが用意）に置き、**経営D・精算Dの両GASが同じセッションを検証する**形にする（ポータルSSO→自動ログイン統合=9/17実装済みの延長。**ポータルで1回ログインすれば経営D・精算Dとも開き、勝手にログアウトしない**が完成形）
2. **ログイン経路自体の粘り強化**: `supalogin`と初回ロードにも404/405リトライ（最大5回・計15秒）＋「接続が混み合っています。自動で再試行中…」の表示（無言の失敗禁止）。リトライ超過時も入力値を保持して再試行ボタン
3. **安全策必須**: 新旧併用フラグ（Supabase優先→無ければProperties参照）・既存ログイン中ユーザーを切断しない移行・ロールバック可能。本番切替はユーザーと実機確認しながら。精算D側のGAS変更もA管轄（デプロイ手順書付きでユーザーへ）
- 効果測定: 切替前後1週間の「ログイン失敗率・強制ログアウト回数」をkd_perf_logで比較しユーザーへ報告

### A-12: PL管理タブの表示をkd_直読みへ前倒し切替（「BQ待ち」からの組み立て直し）
- **方針転換（司令塔決定）**: 従来の切替条件「新旧突合の完全一致後」を待たず、**表示の主経路を`kd_pl_monthly_summary`（Supabase・0.1〜0.3秒）に切替**。旧GAS/BQ経路は裏で突合し続け、差異がある月だけ「⚠️確定処理中（精算書反映待ち）」バッジを出す=**速く出す・確からしさはバッジで示す**
- 理由: PLの遅さ・不安定の主因はGAS+BQ経由。突合の残差異は原因特定済み（業務委託精算4店舗分=C側連携が進行中・TK-86のユーザーデプロイ待ち）で、**表示を待たせる理由にならない**
- あわせて: 目標管理・推移分析など他のBQゲートタブもSWR徹底（sync_run_id不変なら再取得ゼロ）。旧経路フォールバックフラグ維持
- **P支援**: kd_pl_monthly_summaryのリフレッシュ**毎時化**（判定_2026-09-11 §2-3の未了分・先行必須）＋kd_store_monthly（TK-63）完成

### A-13: 「BQで止まらない」構造の明文化（全画面共通・恒久ルール）
**表示はSupabase kd_のみから読む。BQは夜間/毎時のリフレッシュ元に徹する**（リフレッシュが失敗しても画面は前回値＋鮮度表示で動き続ける=BQ不調が画面停止に直結しない）。未移行画面（入金・媒体・広告・明細分析）も順次この形へ（TK-61）。

## 2. 🥈次点「精算書のスピードアップ」（担当A・§1の後すぐ）

### A-14: 精算ダッシュボードの計測→高速化
1. `kd_perf_log`の計測フックを精算D（seisan）にも導入（app='seisan'。invoices/keieiと同型）→遅い処理Top10を実測
2. クイック改善: SWRキャッシュ・自動リトライ（404/405対応をtori-dashboardからseisanのapi層へ移植）・初期表示の読み分割
3. 実測で「シート全読みが主因」と出たら、精算データのSupabaseミラー（kd_seisan_monthly等）をPと設計→表示直読み化（PL方式と同じ）
- 前提知識: 精算GASにも同じ一過性不調あり（TK-180で確認・pingは正常）。A-7（精算UI刷新・モック準拠）は本高速化の後に実施

## 3. ユーザーへのお願い（4点）

1. **実機確認①**: 経営ダッシュボードのログイン安定（9/17のリトライ改善後、勝手にログアウトが減ったか体感を教えてください。A-11の効果測定の基準になります）
2. **実機確認②**: 会計タブの切替速度（請求書メール↔請求書↔会計ダッシュボード↔給与仕訳。9/17修正の確認）
3. **TK-86**: 業務委託精算のGAS手動デプロイ＋実機確認（設計書11章手順・担当Cから依頼済み）→PLの残差異が消え、A-12のバッジが外れます
4. TK-53/54（個人評価・不動産SKの設計回答）は引き続きお待ちしています（急ぎでなければ後回しでOK）

## 4. 貼り付け文（ラウンド6）

```
あなたは担当A（ダッシュボード）です。ns-portal/docs/実装指示書_ラウンド6_2026-09-18.md §1の最優先パッケージを実行してください（2026-09-18スコープ強化版）: A-11（セッションのSupabase一本化=経営D・精算Dの両GASが同じセッションを検証し「ポータルで1回ログインすれば全部開く・勝手にログアウトしない」を完成形に。あわせてsupalogin/初回ロードにも404/405リトライ＋再試行中の表示=「そもそも開けない」対策。Pのテーブル準備後、新旧併用フラグで段階切替・本番切替はユーザーと実機確認・前後1週間の失敗率をkd_perf_logで比較報告）→A-12（PL管理タブをkd_pl_monthly_summary直読みへ前倒し切替。旧経路は裏突合＋差異バッジ「確定処理中」方式。SWR徹底）→A-13（表示はSupabase kd_のみ・BQはリフレッシュ元に徹する構造を全画面ルール化）→§2 A-14（精算Dの計測→SWR/リトライ移植→必要ならミラー設計。目標=会計タブ並みの切替速度とユーザー確認済み）。TK-39（権限調査）は中断してよい。すべて旧経路フォールバックフラグ維持。
```
```
あなたはレーンP（プラットフォーム移行専任）です。ns-portal/docs/実装指示書_ラウンド6_2026-09-18.md §1の支援を最優先で実行してください: ①セッション用テーブル（ds_sessions等。token・user・role・stores・expires・RLS/インデックス・古い行の自動掃除）を新設し担当Aへ宣言②kd_pl_monthly_summaryのリフレッシュ毎時化③kd_store_monthly_summary（TK-63）完成→A宣言。完了後、精算ミラー（kd_seisan系）の設計相談にA-14で乗ってください。
```
```
あなたは担当D（勤怠・給与・監視）です。§1はA主担当に引き継ぎ済みです（9/17のリトライ改善・自動ログイン統合の実装知見はWORKLOGにあるのでAから質問が来たら共有を）。あなたはD-9（インフォマート）・TK-64（kd_unresolved隔離の全ジョブ適用）・TK-66（sync_status見える化）・ロケットナウPhase2（Playwright自動DL。ID/PWをユーザーへ依頼）を順に進めてください。
```
```
あなたは担当F（統合ポータル）です。最優先でTK-34（緊急バグ: 個人情報変更申請が誰にも使えない）を修正してください。その後TK-69（システム利用状況の年月切替）→TK-70（サイドバー赤バッジ）→TK-71（ホーム編集のドラッグUI）へ。
```
```
あなたは担当C（請求書・MF連携）です。ユーザー実機確認（請求書タブ切替の体感）の結果を受けてから、TK-73（請求書フロー全面整理: メール=入口/請求書一覧=処理の分離統合）→TK-85（業務委託精算C側の続き・TK-86のユーザーデプロイ後に実データ確認）を進めてください。会計WSの細かい実機フィードバック対応は従来どおり最優先で割り込みOKです。
```
```
あなたは担当B（nippo）です。現行作業（シフトUIv12刷新・給料振込/現金手渡しタスクの仕上げ）を完了させ、ユーザーのスクショ確認を取ってください。完了後はB-16/B-17（スマレジAPI調査→有給申請）へ戻ります。
```
```
あなたは担当H（AI開発コックピット専任）です。ラウンド6を台帳へ反映してください: A-11/A-12/A-13/A-14を新規登録（A-11はP のテーブル準備をunblocksに）・TK-39を保留へ・TK-60/61をA-12/A-13に統合・P/D/F/C/Bの上記タスク状態を更新。
```

---

## 5. 2026-09-19 進捗整理と再指示（司令塔実査・担当A新スレッド向け。§1〜§4の上書きではなく「続き」）

**背景**: 担当Aスレッドがコンテキスト上限で引き継ぎ（`docs/引継ぎ書_2026-09-19_ラウンド6ダッシュボード安定化.md`）。司令塔が全リポジトリ・WORKLOG・コックピット・実コードを実査し、§1〜§2の現在地を確定した。

### 5.1 現在地（2026-09-19 15:00時点）

| 項目 | 状態 | 残り |
|---|---|---|
| A-11a 経営D側セッションSupabase化 | ✅完了（`ds_sessions`・`SESSION_BACKEND=supabase`切替・実機確認済み） | 効果測定（切替前後1週間の失敗率比較）のみ |
| A-11b **精算D側**の共通セッション | ❌未着手 | 下記5.2-②（着手可能。前提が揃っていることを司令塔が確認済み） |
| A-11c supalogin/初回ロードのリトライ＋再試行表示 | ✅経営D側は完了（1→2→4→8秒・`onRetry`表示） | 精算D側は未（`index.html`の`api()`は0.5→1.5秒の2回止まり）→5.2-② |
| A-12 PL管理タブkd_直読み | 🔶半分（`fetchPlKd_`・検証パネル・差異バッジは本番済み。**KPIカード/PL表の主経路切替は保留**） | `D.pl.filter(r=>!r.store)`で全社共通経費行の有無を確認→主経路切替→5.2-③ |
| **kd_直読みページング重大バグ** | 修正・デプロイ済み（tori `be64cf6`・app.js v201） | **SSO統合アカウントでの実機確認（ユーザー）**→5.4-① |
| A-13 表示はkd_のみ・BQはリフレッシュ元 | ❌未着手（TK-61） | 5.2-④ |
| A-14 精算書スピード | 🔶計測フックは**既に導入済み**（2026-09-02・`app='seisan-dashboard'`でkd_perf_logへ送信中） | Top10実測→リトライ移植→ミラー要否判断→5.2-⑤ |
| P支援（ds_sessions・kd_pl毎時化・TK-63） | ✅3点とも完了・本番稼働（9/18） | コックピットの状態更新（H） |
| D ロケットナウPhase2 | ✅**既に完成済みと判明**（毎朝06:25/毎月5日の自動実行・ID/PWもMac mini運用中） | §4のD貼り付け文の「Phase2着手・ID/PW依頼」は**取り消し**（重複指示）。残=セントラルキッチンの扱い→5.4-④ |
| 運営委託費PL未反映（C→D→A/P） | ✅担当Aが根本原因2件を修正（GH Actionsリトライ・contentType）→**担当C新スレッドが9/19に解決済みと確認・クローズ** | 残=秋葉原肉寿司4件（GOSSO 702×3・榎本 700×1）の「状態を更新」再実行がまだ→5.4-⑤ |

### 5.2 担当Aへの再指示（この順で。すべて旧経路フォールバックフラグ維持）

**① まず**: ユーザーへ5.4-①（SSO実機確認）を依頼し、結果を待つ間に②へ進む（直っていなければ①最優先で追加調査）。

**② A-11b 精算D側の共通セッション化（司令塔が前提を確認済み・今すぐ着手可）**
- **訂正**: 前スレッド引継ぎ書の「ライブのコードをユーザーに貼ってもらう必要がある」は不要。**`~/seisan-dashboard/gas/SeisanDashboard.gs`（リポジトリ mirai-oss/seisan-dashboard・`SD_VERSION='v5.20-plsync-monthkey-fix'`・最終コミット2026-09-10）がライブと同一版**であることを、司令塔がGASの`?action=ping`で確認した（`{"ok":true,"ver":"v5.20-plsync-monthkey-fix"}`）。`~/NStyle-AI/gas-backup/seisan/`（v5.14）は使わない。
- **真因**: 精算Dのセッションは`CacheService.getScriptCache()`に`sdtk_<token>`で保存（`sd_login`/`sd_supaLogin`・**6時間固定・延長なし・キャッシュは予告なく消えることがある**）→「ログイン後に勝手にログアウト」はこれ。経営D（PropertiesService）より弱い構造。
- **やること**（経営D `gas/Code.gs` 535〜620行の`sessionPut/Get/Del`＋`SESSION_BACKEND`フラグをそのまま移植する形で）:
  1. `sd_login`/`sd_supaLogin`のセッション発行時に、CacheServiceに加えて`ds_sessions`（同じテーブル）へも書く（token主キー・`sess`jsonbに`{app:'seisan', id, name, role, storesRaw, email}`・`expires_at`）。**`email`を必ず入れる**（共通セッションの突合キー）。
  2. `sd_auth_`は「CacheService→無ければ`ds_sessions`→あればCacheServiceへ再投入・`expires_at`を使うたび延長（sliding TTL・経営Dと同じ時間）」。`ds_sessions`にも無ければ従来どおり`AUTH`エラー。
  3. **共通セッション（ポータル1回ログインで両方開く）**: `ds_sessions`の行が経営D発行（`sess.app!=='seisan'`）でも、`sess.email`が`権限_精算ダッシュボード`シートのメール欄に一致し・有効・役職が本部/マスターなら精算Dのセッションとして受け入れる（`sd_supaLogin`の突合ロジックを再利用。委託先は従来どおり対象外）。経営D側`supaLogin`が`sess`に`email`を保存していない場合はA-11aの`sess`へ`email`を追加する（Aの管轄・Pのテーブル変更は不要）。
  4. 新旧併用フラグ`SESSION_BACKEND`（スクリプトプロパティ・既定`cache`=従来動作・`supabase`で上記有効）。経営Dと同じ名前・同じ意味にする。
  5. `index.html`の`api()`リトライを経営D方式へ揃える（1→2→4→8秒・最大4回・GASがHTMLページを返す404/405もリトライ対象・`onRetry`で「接続が混み合っています。自動で再試行中…」表示・超過時は入力値保持＋再試行ボタン）。**これはA-14の「クイック改善」も兼ねる**。
  6. 精算Dのスクリプトプロパティに`SUPABASE_URL`/`SUPABASE_SERVICE_KEY`が必要（service_role限定RLSのため）。**値の投入はユーザー本人**（Aは手順書を書き、値は扱わない）。プロパティ一覧の読み取り専用化問題（経営Dで発生）に備え、一時関数1回実行方式の手順も併記。
  7. ライブへの反映はユーザー手貼り。**全文貼替はしない**（関数単位・Cmd+Fのユニーク文字列指定。落とし穴#3）。切替はユーザー立会いで`SESSION_BACKEND=supabase`→実機確認→問題があれば`cache`へ戻すだけ。
- 完了条件: ポータルでログイン→経営D・精算Dの両方が開く→6時間超放置してもログアウトされない→kd_perf_logの`AUTH`/ログイン失敗が切替前後1週間で減少（報告）。

**③ A-12 主経路切替**: `D.pl.filter(r=>!r.store)`（PL画面ロード後のコンソール）で全社共通経費行の実在を確認→無ければそのままKPIカード・PL表の主経路を`kd_pl_monthly_summary`へ（フラグ付き）。あれば差分をPへ報告してから切替。**新規fetchXxxKd_は`fetchAnalysisKd_`の現行実装（空配列で終了・受信件数ぶんoffset）をコピー**すること。

**④ A-13**: 入金・媒体・広告・明細分析の順に「kd_直読み・GAS非経由・前回値＋鮮度表示」へ移行（TK-61）。ルール文は`docs/`に1ページ（「表示はSupabase kd_のみ・BQはリフレッシュ元」＋ページング/SWR/フォールバックの型）を残す。

**⑤ A-14**: 計測は導入済みなので**新設しない**。レーンPから`app='seisan-dashboard'`の遅い処理Top10（直近7日）を受け取り（5.3 P）、②-5のリトライ移植と合わせて実測で「シート全読み」が主因なら`kd_seisan_*`ミラーをPと設計（設計書ドラフト→司令塔経由でユーザー確認→実装）。合格ライン=会計タブ並み。

### 5.3 他レーンへの追記指示

- **レーンP**: ①`kd_perf_log`から`app='seisan-dashboard'`の直近7日Top10（action別の平均/最大ms・失敗率）を抽出して担当Aへ（WORKLOG＋SendMessage）②`ds_sessions`は経営D/精算D共通のまま変更不要。`sess`の中身（`app`/`email`）はAが決めるので、Pはインデックス/掃除ジョブが両方の行に効くことだけ確認③A-14のミラー設計相談に乗る。
- **担当D**: ロケットナウPhase2は完成済みのため§4の該当指示は取り消し。5.4-④の回答が来たら`delivery_store_map`登録。他はD-9・TK-64残2ジョブ・TK-66継続。
- **担当C**: 9/19新スレッドで①精算書PL未反映=解決済み確認・③TK-92/93=done反映済み（司令塔確認）。残は5.4-⑤（秋葉原肉寿司4件の「状態を更新」再実行）の結果確認と、売上入金の一連の流れ・給与仕訳月表示ガードの実機確認待ち。その後は§4のC指示（TK-73→TK-85）へ。
- **担当H**: 台帳反映=5.5参照。

### 5.4 ユーザーへのお願い（番号で返答OK）

1. **SSO実機確認（最重要）**: ポータル経由で経営ダッシュボード→「推移分析」→店舗「鶏武者 新横浜」×「年初来」で、**今年の月別売上が全月出るか**（出ない月があればスクショ）。9/19修正の確認です。
2. **精算Dのスクリプトプロパティ2件**（`SUPABASE_URL`・`SUPABASE_SERVICE_KEY`）: 担当Aから手順書が届いたら投入をお願いします（値は担当Aに渡さず、ご自身でApps Script画面へ）。
3. **TK-86**（業務委託精算GASデプロイ・設計書11章）: 引き続きお願いします（PLの残差異が消え、A-12のバッジが外れます）。
4. **セントラルキッチン**を売上ダッシュボードの「店舗」として扱いますか？（a=登録する／b=調理場なので対象外）
5. 精算書で**GOSSO株式会社（伝票702）・榎本酒類（伝票700）**の「状態を更新」を1回押してください（9/17修正前の「PLエラー」表示の残骸を消す確認）。
6. 経営Dのログイン体感（`SESSION_BACKEND=supabase`切替後、勝手にログアウトが減ったか）を一言ください。

### 5.5 コックピット台帳への反映依頼（担当H）

- 新規: **A-11b**（精算D共通セッション・担当A・high）／**A-13**（TK-61に統合可）／**A-14**（精算書スピード・担当A）。A-11a=完了として登録。
- 状態更新: TK-63→done（P・9/18）／TK-60→A-12へ統合（A-12はin_progress・主経路切替待ち）／TK-61→A-13として継続／ロケットナウPhase2=完了（D）。
- 待ち: 5.4-①〜⑥をwaiting_human（中山）で登録（①と②はA-11b/ページング確認をunblocks）。

### 5.6 貼り付け文（2026-09-19版。A/P/D/C/Hは§4の同レーン文と差し替え。B/Fは§4のまま）

```
あなたは担当A（ダッシュボード）の新スレッドです。まず ns-portal/WORKLOG.md冒頭「📍現在の状況」→ ns-portal/docs/引継ぎ書_2026-09-19_ラウンド6ダッシュボード安定化.md → ns-portal/docs/実装指示書_ラウンド6_2026-09-18.md §5（2026-09-19 司令塔の再指示）の順に読んでください。§5.2の順で実行: ①ユーザーへSSO実機確認（鶏武者 新横浜×年初来）を依頼し結果を待つ間に②へ ②A-11b 精算D側の共通セッション化（訂正: ライブのソースは ~/seisan-dashboard/gas/SeisanDashboard.gs v5.20 で司令塔がping確認済み・ユーザー貼り付け不要。真因=CacheService 6時間固定。経営D Code.gs 535〜620行の sessionPut/Get/Del＋SESSION_BACKEND を移植・ds_sessions共通・sessにemail必須・index.htmlのリトライを1→2→4→8秒＋再試行中表示へ。SUPABASE_URL/SERVICE_KEYの投入はユーザー本人=手順書のみ。全文貼替禁止・関数単位で手貼り依頼）③A-12 D.pl.filter(r=>!r.store)確認→KPI/PL表の主経路をkd_へ（フラグ付き・fetchAnalysisKd_のページング実装をコピー）④A-13 入金・媒体・広告・明細分析をkd_直読みへ＋ルール文書1ページ ⑤A-14 計測は導入済み(app='seisan-dashboard')なので新設せずPからTop10を受け取り、ミラー要否を判断（合格=会計タブ並み）。旧経路フォールバック維持・WORKLOG 📍先頭＋ai-cockpit progress を節目ごとに。
```
```
あなたはレーンP（プラットフォーム移行専任）です。ns-portal/docs/実装指示書_ラウンド6_2026-09-18.md §5.3 のP分を実行してください: ①kd_perf_logから app='seisan-dashboard' の直近7日Top10（action別 平均/最大ms・失敗率）を抽出し担当AへWORKLOG＋SendMessageで渡す ②ds_sessionsは経営D/精算D共通のまま（変更不要）。sessにapp/emailが入る前提で、インデックスとsessions_cleanupが両アプリの行に効くことだけ確認 ③担当AからA-14の精算ミラー(kd_seisan_*)設計相談が来たら乗る。
```
```
あなたは担当D（勤怠・給与・監視）です。ラウンド6§4のD向け指示のうち「ロケットナウPhase2着手・ID/PW依頼」は完成済みと判明したため取り消します（§5.1）。ユーザーからセントラルキッチンの扱い（§5.4-④）の回答が来たら delivery_store_map を登録してください。他はD-9・TK-64の残2ジョブ・TK-66を継続。
```
```
あなたは担当C（請求書・MF連携）です。ns-portal/docs/実装指示書_ラウンド6_2026-09-18.md §5.3 のC分を確認してください: 9/19のオープン課題確認（①解決・③done）は司令塔が把握済みです。残りはユーザーが秋葉原肉寿司4件の「状態を更新」を再実行した結果の確認（§5.4-⑤）と、売上入金フロー・給与仕訳月表示ガードの実機確認待ちのみ。並行して§4のC指示（TK-73 請求書フロー整理→TK-85 業務委託精算C側）へ進んでください。
```
```
あなたは担当H（AI開発コックピット専任）です。ns-portal/docs/実装指示書_ラウンド6_2026-09-18.md §5.5 を台帳へ反映してください: A-11a=done／A-11b・A-14を新規（担当A・high）／A-13=TK-61に統合して継続／TK-60→A-12に統合／TK-63=done（P・9/18）／ロケットナウPhase2=done（D）／§5.4の①〜⑥をwaiting_human（中山）で登録（①②はA-11b等のunblocks）。
```

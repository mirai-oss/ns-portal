# 復旧の入口（「壊れたらまずここを開く」1枚）

作成: 2026-08-27（担当D） ／ 元: `docs/設計書_全社バックアップと復旧_2026-08-27.md`（D-5a・柱1）
目的: **「◯◯が壊れたら → ここを開く → この手順」** をこの1ページだけで完結させる地図。データそのものではなく「確実にたどり着ける場所」。

**この文書の使い方**: 事故が起きたら、下の表で壊れたものの行を探す →「復旧手順」列のリンクを開く → 手順どおりに進める。**DB全体を戻す・ファイルを一括操作するような取り返しのつかない操作は、必ず先にユーザーの判断を仰いでから実行する**（Claudeが単独では行わない）。

---

## 1. 一覧表（正本・バックアップ・復旧手順・所要時間）

| # | データ・システム | 正本の場所 | バックアップ | 復旧手順 | 所要時間の目安 |
|---|---|---|---|---|---|
| 1 | 業務データ（日報・シフト・本部タスク・請求書・応募者・経費など） | ハブSupabase `uuvsxzhpxtghojoubjcc`（東京リージョン） | ✅**日次自動**（直近7日分。2026-08-27時点で8/20〜8/26の7日分`COMPLETED`を確認済み）。PITR（秒単位巻き戻し）は未加入 | [`docs/バックアップと復旧.md`](./バックアップと復旧.md)（Supabaseダッシュボードからの復旧手順） | 数分〜数十分（DBサイズ次第）。**復旧中は全システム停止** |
| 2 | 社内秘密情報（給与体系・借入・保管書類の索引など） | **同じハブSupabase `uuvsxzhpxtghojoubjcc`の`info`スキーマ**（2026-08-18に本番切替済み。旧・専用プロジェクト`wciefkpooncglahqdtmu`は当面残置＝二重の安全網。削除は2026-09-01頃ユーザー判断） | ✅#1と同じ日次自動バックアップに含まれる（`info`スキーマもプロジェクト内なので対象） | #1と同じ（`docs/バックアップと復旧.md`） | #1と同じ |
| 3 | **写真・添付ファイル**（`report-photos`＝日報写真、`invoice-files`＝請求書添付、`manual-files`、`export-outputs`/`export-templates`、ns-info-system側の`documents`／`photos`＝応募者写真・保管書類） | Supabase Storage（上記と同じ2プロジェクト） | ❌**現状バックアップなし**（DBの日次バックアップの対象外）。**D-5bで週次オフサイトコピーを追加予定（未完了）** | 復旧手段なし。誤削除したファイルは基本的に戻せない。**現状、全社で最も弱い箇所** | 復旧不可（対策＝日頃の削除を慎重に。D-5b完了後は直近世代から拾える） |
| 4 | 売上・広告・PLの分析データ | BigQuery `tori-analytics`（各データセット） | ❌スナップショットなし（方針として「持たない」） | 元データ（Google Sheets／ハブSupabase／各種取込ジョブ）から**再生成**。既存の`bqSyncSales`等の同期コマンドを再実行 | 数十分〜（データ量・対象期間による） |
| 5 | 売上・PLの元データ（各種スプレッドシート） | Google スプレッドシート | ✅Google Drive標準の版履歴 | スプレッドシートの「変更履歴」から復元 | 数分 |
| 6 | プログラムコード（各システム） | GitHub `mirai-oss`組織配下7リポジトリ: `NStyle-AI` / `ns-portal` / `ns-daily-import` / `ns-info-system` / `nippo` / `tori-dashboard` / `n-style-projects` | ✅Git履歴そのものがバックアップ | 対象コミットへ`git checkout`／`revert`。リポジトリ自体が消えてもGitHub側に復元機能あり | 数分 |
| 7 | GASコード（ダッシュボード・PL・精算・広告管理・取込） | 各Google Apps Scriptプロジェクト | ✅手動バックアップ運用：`NStyle-AI/gas-backup/`配下（`ad-mgmt`／`dashboard`／`dashboard-server`／`pl-system`／`seisan`／`sales-db`／`review-db`） | `gas-backup/<プロジェクト>/README.md`にスクリプトID記載 → `clasp`で該当GASプロジェクトへ貼り戻す、または手動コピペ | 十数分。**注意: 更新を忘れると古い版しか残らない**（コード変更のたびに`gas-backup`へ反映する運用の徹底が対策） |
| 8 | 各種シークレット（APIキー・トークン類） | `app_secrets`テーブル（ハブSupabase内＝#1の対象）／GitHub Secrets／GAS Script Properties／Mac mini `.env`（`ns-daily-import`）・`~/.config/ns-portal/`配下 | `app_secrets`は#1のDB日次バックアップに含まれる。GitHub Secrets・Script Properties・Mac mini`.env`は❌**バックアップなし・散在**。**「何がどこにあるか」の一覧も現状なし** | 個別に再発行・再設定する以外に手段なし（現状） | 案件ごとに異なる。**D-5bの柱3（Mac mini対策）で暗号化した控えの保管手順を整備予定（未完了）** |
| 9 | Mac miniの取込ジョブ（`ns-daily-import`）と`.env` | Mac mini実機のみ | ❌**バックアップなし**。コード自体はGitHub（#6）にあるが、`.env`（ログイン情報・トークン類）は実機のみ。**2026-08-22に実機再起動だけで取込が全停止した実績あり＝単一障害点** | Mac mini故障時: コードは`git clone`で復元可能だが、`.env`の再構築（各サイトへの再ログイン等）が必要 | 半日〜（各サイトの再ログイン・認証情報の再取得次第）。**D-5bの柱3で対策予定（未完了）** |

---

## 2. 今回の検証で分かったこと（2026-08-27・担当D確認）

- **#1・#2は実は同じSupabaseプロジェクト**。2026-08-18の本番切替により「社内秘密情報＝別プロジェクトで要確認」ではなく、ハブと同じ日次バックアップの対象内であることをManagement API（`GET /v1/projects/uuvsxzhpxtghojoubjcc/database/backups`）で確認した（2026-08-20〜08-26の7日分が`COMPLETED`、`walg_enabled: true`、`pitr_enabled: false`）。設計書§1の「機密データ＝要確認」は本書により解消。
- **最大の穴は#3（写真・添付ファイル）と#8・#9（シークレット・Mac mini）**。この3つはD-5bで対策予定だが、本書作成時点では**未完了**。D-5b完了後、本表の該当行を更新する。
- 旧・社内情報プロジェクト（`wciefkpooncglahqdtmu`）は**まだ削除されていない**（2026-09-01頃ユーザー判断予定）ため、#2は当面二重の安全網がある状態。

## 3. この文書の更新ルール

- D-5b（週次オフサイトバックアップ）が完了したら、#3・#8・#9の行を更新すること。
- 新しいシステム・新しいバケット・新しいリポジトリが増えたら、この表に追記すること（会社のシステムが増えるたびに更新する「生きた地図」として運用する）。

---

**次のアクション**: このドキュメントが完成したので、統合ポータルの⚙️管理グループからのリンク追加を担当Fへ依頼する（WORKLOG経由）。

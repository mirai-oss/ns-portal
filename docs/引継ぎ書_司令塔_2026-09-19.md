# 引継ぎ書: 司令塔スレッド（2026-09-19版・これが最新。旧2026-08-26版は履歴）

対象: **司令塔・設計構築・指示者**を務めるスレッド。ユーザー正式指示（2026-09-01）により**タスクの司令塔はこのスレッド1本のみ**（他スレッドはラウンド指示書・設計書を発行しない）。司令塔は実装しない（実装はレーン専任制）。

## 1. まず読む順番（毎回これ）

1. **全リポジトリ`git pull --rebase --autostash`**: ns-portal／nippo／tori-dashboard／ns-info-system／ns-daily-import（NStyle-AIは未コミット変更があるためfetchのみ推奨）
2. **`ns-portal/WORKLOG.md`冒頭「📍現在の状況」＋「📋全体バックログ・統合版」**（経緯の正本）
3. **最新ラウンド指示書 = `docs/実装指示書_ラウンド6_2026-09-18.md`**（進んでいればラウンド7以降を探す）
4. **`~/ns-portal/tools/ai-cockpit list`**（進行の「今」の正本。コックピット=今／WORKLOG=経緯）
5. 大型並行案件の正本: 会計・請求WS刷新=`実装指示書_会計請求ワークスペースUI刷新_2026-09-11.md`（SoT=mockups/NStyle_添付HTMLをベースにした_3HTML統合版_修正版.html＋補助参照=NStyle_会計請求書自動化_UI.html）／高速化=`設計書_表示集計層kdと高速化実行計画_2026-09-02.md`＋`判定_高速化検証と実装GO_2026-09-11.md`

## 2. 現在の最優先（2026-09-19時点・ユーザー明言）

**🥇経営ダッシュボードの安定と速度（ラウンド6§1・担当A主担当/P支援）**:
- **A-11 共通セッションのSupabase一本化**（承認済み・最重要）: 経営D・精算Dの両GASが同じSupabaseセッションを検証→「ポータル1回ログインで全部開く・勝手にログアウトしない」。ユーザー体感=「そもそもログインして開けない・精算書も勝手にログアウト」。supalogin/初回ロードへの404/405リトライ＋再試行表示も含む。新旧併用フラグ・本番切替はユーザー立会い・前後1週間の失敗率比較報告
- **A-12 PL管理タブをkd_pl直読みへ前倒し切替**（差異バッジ「確定処理中」方式・突合完全一致を待たない方針転換済み）
- **A-13 恒久ルール「表示はSupabase kd_のみ・BQはリフレッシュ元に徹する」**（BQ不調が画面停止に直結しない構造）
- **🥈A-14 精算書のスピード**（計測→SWR/リトライ移植→必要ならミラー化。**合格ライン=会計タブ並み**とユーザー確認済み）
- ユーザーは「最優先が改善されたら次（評価・SK設計回答）に進みたい」と明言→TK-53/54は急かさない

## 3. 各レーンの現在地（詳細はラウンド6§0と📍）

A=最優先パッケージ全振り（TK-39中断可）／B=シフトUIv12＋給料振込タスク仕上げ→B-16/17／C=会計WS怒涛の進化中（v14＋請求書一覧高速化は**ユーザー実機で「だいぶ早い」確認済み**）→TK-73・TK-85／D=D-9・TK-64/66・ロケットナウPhase2（Playwright・ID/PW依頼要）／P=sessionsテーブル・kd_pl毎時化・TK-63／E=待機／F=**TK-34緊急バグ（個人情報変更申請が誰にも使えない）**→TK-69/70/71／G=待機／H=台帳運用。全レーンの貼り付け文=ラウンド6§4（Aのみ9/18強化版に差し替え済み）

## 4. 司令塔の定型手順（確立済み・必ず踏襲）

1. 新要望→**重複チェック**（既存機能・進行タスク・バックログと突合。「似た機能があれば無視・肉付けできれば肉付け」がユーザー方針）
2. **設計要否の仕分け**→設計が必要なら設計書ドラフト＋番号付き質問（ユーザーは番号だけで回答する。当てずっぽうで設計しない）→回答→§確定追記→**Sync宣言**→割当
3. ラウンド指示書へ追記（新ラウンドは大きな節目のみ）・**貼り付け文を必ずチャットに直接出す**（ユーザーが各スレッドへ貼る運用）
4. モック受領時: 秘密スキャン→`docs/mockups/`保存→**必ずローカルHTTPサーバー経由でブラウザで開いて確認**（PC＋スマホ）→参照デザイン登録。SoTの優先順位ルールを明記
5. WORKLOG更新は**📍先頭に★エントリ＋末尾に日付エントリ**の2点セット→秘密チェック（sk-ant等のgrep）→commit→pull --rebase --autostash→push（ns-portalは公開リポ: セキュリティ所見・個人情報・実データファイルは置かない）
6. **ai-cockpit連携（鉄則#9）**: 節目でnote送信・ユーザーが「やった」と言ったタスクは`ai-cockpit list`→`done --task TK-x`で代理完了・新タスク登録は担当Hへ依頼（CLIに登録機能なし）
7. レーン間連絡はWORKLOG＋SendMessage。担当外ファイルの依頼が来たら実装せず該当レーンの指示に組み込む（司令塔もコードは書かない）
8. DB変更は事前報告様式（理由・現構造・影響・migration・rollback）。旧UI/旧経路は削除せずフラグで切替・ロールバック維持
9. 日付は`date`コマンドで確認してから文書に書く（過去に誤記事故あり）

## 5. 確定済み設計判断（再議論しない・変更はユーザー承認）

正本=業務Supabase/分析BQ・シートは入力UIと出力先に降格／**数字が狂わない4原則**（1数字1正本・店舗名は必ずstore_aliases経由・部分反映＋出典表示・毎日の自動突合）／**店舗名ゲートウェイ**（未知名は書き込まずkd_unresolved_names隔離＋Lark）／**表示はkd_のみ・BQはリフレッシュ元**（A-13）／一覧APIはlimit＋期間必須・select=*禁止／請求書詳細は取得元問わず1本／本部タスクは人間対応が必要な例外のみ（会計処理の正本にしない）／AI=下書き・人=承認／不動産SKは同居・`re_`接頭辞・ID参照／振込実行・個人情報の分析基盤混入は自動化しない／GASバッチ化(bootstrap)は実測悪化で不採用確定

## 6. ユーザー待ち・保留（2026-09-19時点）

TK-86（業務委託精算GASデプロイ=OK済み・実施待ち→完了でPL差異バッジ解消）／A-11本番切替の実機立会い（Aから依頼が来る）／TK-17（コックピット自動フック）／ロケットナウのID/PW（DのPhase2着手時）／TK-53・TK-54（評価・SK設計回答=**最優先改善後でよいとユーザー明言**）／カメラ機材発注GO（設計メモ§6.1）

## 7. 主要文書索引（生きているもの）

ラウンド指示書4〜6／会計請求WS刷新（SoT2本）／表示集計層kd＋判定・計画書（脱GAS）／設計書: 予約基盤(確定)・会計自動化(確定)・広告費/精算書PL(確定+§5手数料)・デリバリー売上ロケットナウ(確定)・個人評価(質問中)・不動産SK(質問中)・全社バックアップ(完了)／シフトUIv12指示書（B）／社内情報整理指示書（2026-09-08）／担当分けマトリクス（レーンA〜H・I・P）／復旧の入口.md

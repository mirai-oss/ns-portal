# 提案: 社内情報管理システム（ns-info-system）の整理整頓・横断連携・UI改善

作成: 2026-09-08 ／ 対象: `mirai-oss/ns-info-system`（本番 https://ns-info-system.vercel.app ／ DBはハブSupabaseの`info`スキーマ）
位置づけ: **提案書（実装前）**。採否と優先順位はユーザーが決める。採用分は実装指示書に落として担当スレッドへ渡す。

---

## 0. 現状の把握（GitHubのコードから確認した事実）

| 観点 | 現状 |
|---|---|
| 画面・カテゴリ | 12カテゴリが横並び: 法人情報／店舗情報／物件・契約／銀行・口座／借入金／取引先／ID・PW／広告媒体／従業員（基本）／従業員（給与）／通信・水道光熱／不動産。ほかに 書類・写真・期限の管理・検索・通知・変更申請 |
| 店舗情報 | 店舗コード・店名・エリア・住所・店長・電話・営業時間・緊急連絡先・URL・ポスト情報・メモ（`info.stores`。店舗・法人マスタはハブと外部キーで統一済み） |
| 物件・契約 | 店舗・賃料（税抜/月）・坪数・契約種別・契約日・更新日・保証金・償却・管理会社・大家（`info.properties`）。**初期投資の項目は無い** |
| 従業員 | 基本情報（氏名〜国籍）＋給与体系（時給/固定給/賞与/交通費）を `info.employees`／`info.employee_salaries` に持つ。**基本情報はnippo（`users`/`employee_profiles`）と二重管理**。給与明細（月次の支給実績）を入れる場所は無い |
| 取引先 | `info.suppliers`（連絡先台帳）と、ポータル側 `public.vendors`（請求書処理・振込口座）の2つ。2026-09-08に「手動で紐付けて参照表示」の軽い連携のみ実施 |
| 期限の管理 | `info.deadlines` は **タイトル・期限日・メモ・店舗** だけ。種類はタイトルの文字列から推測（「許可」→営業許可証）。物件の更新日は登録時に手で `deadlines` にもコピーしている |
| PL連携 | 借入返済のみ `info.loan_repayment_monthly` → 経営D（F-8）。家賃は経営PL（`kd_pl_monthly_summary.rent`）が **DB_PLの手入力（R区分）** を見ており、物件契約の賃料とは未接続 |
| ポータルとの連携 | ホームの「⚠️要対応（横断）」ウィジェットは 日報・本部タスク・チェックシート等が対象。**社内情報の期限は流れていない** |
| UI | 青ヘッダー＋白カード。強調色が 青・赤・黄・緑 の4色＋絵文字見出し。トップは「承認待ちバナー → 期限 → 店舗一覧 → 12カテゴリ」の縦積み |

---

## 1. 提案一覧（優先順位つき）

| # | 提案 | 効果 | 工数目安 | 優先 |
|---|---|---|---|---|
| A | **店舗カルテ化**（店舗情報＋物件契約＋ライフライン＋許認可＋ID/PW＋取引先を「1店舗1ページ」に集約。テーブルは分けたまま） | 店舗のことは1箇所で全部見える。二重入力ゼロ | 中 | ★★★ |
| B | **初期投資（開業コスト）テーブル新設**＋店舗別合計の自動算出＋**経営PLへ自動反映**（減価償却・投資回収） | 出店判断・回収期間が数字で見える | 中 | ★★★ |
| C | **期限の自動生成＋ポータルホーム連携**（契約更新・営業許可・在留期限などを元データから自動で作り、重要なものだけポータルに出す） | 期限切れ事故の防止。手入力の重複廃止 | 小〜中 | ★★★ |
| D | **取引先マスタの一本化**（正本を `public.vendors` に統一。支払ルール項目を追加） | 請求書処理・振込タスク・会計連携が1入力で回る | 中 | ★★ |
| E | **従業員: 基本情報はnippo参照、給与明細は社内情報に保管** | 人事情報の二重管理をやめ、給与明細PDFの置き場を確保 | 中 | ★★ |
| F | **家賃をPLへ自動反映**（物件契約の賃料＋共益費が正本。DB_PLのR区分手入力を廃止） | 毎月の手入力が消える | 小 | ★★ |
| G | **UI整理**（色を「基本1色＋警告2色」に絞る・絵文字廃止・12カテゴリを4グループに） | 何が重要か一目で分かる | 中 | ★★ |
| H | データ交通整理表の確定（この情報はここ、の一覧を要件定義書§16に追記） | 以後の迷いを無くす | 小 | ★★★（Aの前提） |

---

## 2. 【A】店舗情報と物件契約を「1つにする」か

**結論: 画面は1つにする。テーブルは分けたままにする。**

- 1店舗に物件契約は複数ありうる（本店＋セントラルキッチン（TOHOビル2F/3F）、移転前後、駐車場・倉庫）。テーブルを1つにすると「店舗1行に契約1つ」しか持てず、移転や契約更新の履歴が消える。
- なので **`stores`（1） : `properties`（N）** の関係は維持し、画面を「店舗カルテ」に統合する。

### 店舗カルテの画面構成（タブ）

```
店舗カルテ: 04 鳥一代 恵比寿店                        [営業中] [営業許可 2027/03/15 まで 残188日]
┌ 基本 ┬ 物件・契約 ┬ 初期投資 ┬ ライフライン ┬ 許認可・期限 ┬ ID/PW ┬ 取引先 ┬ 書類・写真 ┐
```

| タブ | 中身 | 元テーブル |
|---|---|---|
| 基本 | 店名・住所・電話・店長・営業時間・緊急連絡先・URL・ポスト（現行の店舗情報） | `info.stores` |
| 物件・契約 | 現行契約（賃料・共益費・坪数・契約日・更新日・保証金・償却・大家・管理会社）＋過去契約の履歴 | `info.properties`（**共益費・更新料・解約予告期間・原状回復条件** を追加） |
| 初期投資 | 【B】で新設。区分別の明細と合計 | `info.store_investments`（新設） |
| ライフライン | 電気・ガス・水道・通信の契約先・お客様番号 | `info.utilities`（既存。店舗単位に表示） |
| 許認可・期限 | 営業許可・深夜酒類・防火管理者・消防点検・契約更新など、この店舗の期限すべて | `info.deadlines`（【C】で強化） |
| ID/PW | この店舗に紐づくID/PW | `credential_stores`（既存） |
| 取引先 | この店舗で使う取引先（仕入・広告など） | `vendors`（【D】） |
| 書類・写真 | 契約書PDF・許可証・店舗写真 | `documents`/`photos`（`store_id` で絞る） |

- 現行の「店舗情報」「物件・契約」「通信・水道光熱」の一覧画面は、店舗カルテの入口（店舗一覧カード）に置き換える。
- 全店横断で見たいもの（賃料の合計、更新日一覧）は「店舗一覧」の右側に集計として残す。

---

## 3. 【B】初期投資（開業コスト）と経営PLへの反映

### 3.1 テーブル案 `info.store_investments`

| 列 | 説明 |
|---|---|
| store_id / property_id | どの店舗・どの契約に対する投資か |
| category | 区分（下記） |
| amount | 金額（税抜） |
| tax | 消費税 |
| paid_on | 支払日 |
| vendor_id | 支払先（`vendors`） |
| treatment | **資産計上／費用／返還される預け金** の3択（下記） |
| depreciation_years | 耐用年数（資産計上のときだけ。内装15年・厨房機器6〜8年など） |
| document_id | 見積書・請求書・契約書PDF |
| memo | |

**区分（初期値）**: 敷金・保証金／礼金／仲介手数料／償却（保証金の償却分）／内装工事／厨房機器／什器・備品／看板・サイン／POS・通信工事／求人費用（開業前）／開業前人件費（研修）／開業前家賃（フリーレント外）／許認可・登記費用／その他

**treatment の考え方（合計を3つに分けて出す）**

| treatment | 例 | 合計の見せ方 |
|---|---|---|
| 返還される預け金 | 敷金・保証金（償却分を除く） | 「預け金」＝出ていくが戻る |
| 資産計上（減価償却） | 内装工事・厨房機器・什器 | 「設備投資」＝月々の減価償却でPLに乗る |
| 費用 | 礼金・仲介手数料・求人費・開業前人件費・償却 | 「開業費用」＝開業月（または開業費として償却）にPLに乗る |

画面には **「初期投資合計」「うち戻る分」「うち月々の償却額」「回収見込み（合計 ÷ 月次営業利益）」** を自動表示する。

### 3.2 経営PLへの反映ルート

既存の借入返済連携（F-8: `info.loan_repayment_monthly` → 経営D）と同じ型で作る。

```
info.store_investments
   └ ビュー info.v_store_investment_monthly（店舗×年月: 減価償却額・開業費用・投資累計）
        └ keiei-kd-refresh（op=investment_monthly）が読み取り
             └ kd_pl_monthly_summary に depreciation（減価償却）・opening_cost（開業費用）列を追加
                  └ 経営PLボードに「減価償却」「開業費」「投資回収」の行が出る
```

- PLボード側は既に「簡易CF（法人税等・減価償却費・返済元金）は未計算」と記録されている（WORKLOG 2026-09-06）。この提案でその減価償却の穴が埋まる。
- 会計ソフト（MF）の減価償却額と一致させるか、簡易計算にするかは要決定（§9 質問1）。

---

## 4. 【C】期限アラートの自動生成とポータルホーム連携

### 4.1 期限を「手入力」から「元データから自動生成」へ

今は物件の更新日を登録したうえで、別途「期限の管理」にも同じ日付を入れている。これをやめる。

| 期限の種類 | 元データ | 自動生成ルール |
|---|---|---|
| 物件契約の更新 | `properties.renewal_date` | 解約予告期間（例: 6ヶ月前）を逆算して「判断期限」も同時に作る |
| 営業許可証 | `deadlines`（種類=営業許可） | 手入力のまま（元データが無いため）。**種類を選択式**にし、許可証PDFを必須添付 |
| 深夜酒類提供・防火管理者・消防点検・食品衛生責任者 | 同上 | 種類を選択式に追加 |
| リース満了 | 取引先ルール（【D】）のリース契約終了月 | 自動 |
| 借入完済・据置終了 | `loans` | 自動 |
| 在留期限（外国籍従業員） | `employees`（在留期限列を追加） | 自動。**90日前**に人事へ |
| 保険更新 | `deadlines` 手入力 | 種類を選択式に追加 |

`deadlines` に追加する列: `kind`（選択式）／`severity`（重要・通常）／`remind_days`（何日前に知らせるか。種類ごとの既定値）／`source_table`・`source_id`（自動生成元）／`document_id`／`done_at`（更新済み）。

### 4.2 ポータルホームへの連携（重要なものだけ）

- ポータルの「⚠️要対応（横断）」ウィジェット（`panel_alerts`）に **社内情報の期限** を1つの情報源として追加する。
- `info` スキーマはRLSが別なので、ポータルからは **RPC `info_alerts_for_portal()`**（security definer）1本で取る。返すのは「severity=重要 かつ 期限まで60日以内 or 超過」のみ。役割で絞る（マスター・本部＝全店、店長＝自店のみ）。
- 表示例: `🔴 恵比寿店 営業許可証 あと12日` → クリックで社内情報の店舗カルテ「許認可・期限」タブへSSO遷移（既存の `/sso?to=` を使う）。
- 通知: 既存の本部タスクの通知経路（Chatwork）に「期限の30日前・7日前・当日」を流す。社内情報側の通知画面は残す。

**ポータルに出す／出さない の線引き（初期値）**

| 出す（重要） | 出さない（社内情報の中だけ） |
|---|---|
| 営業許可・深夜酒類・防火管理者の期限 | 保険更新・ドメイン更新などの軽微なもの |
| 物件契約の解約予告期限・更新期限 | 完了済み・90日より先のもの |
| 在留期限（90日以内） | |
| 承認待ちの変更申請（既にバナーあり） | |

---

## 5. 【D】取引先マスタの一本化

**結論: 正本を `public.vendors`（ポータル側）にする。社内情報の取引先画面は `vendors` を表示・編集する画面に差し替え、`info.suppliers` は移行後に廃止。**

理由: 請求書処理・振込口座・PL反映・本部タスク（振込）の起点が `vendors` にあり、連絡先台帳（`suppliers`）より役割が重い。同じSupabase内なので画面の差し替えだけで済む。

### `vendors` に追加する項目（今回作成した「取引先ルール一覧」をそのまま持てる形）

| 項目 | 例 |
|---|---|
| 連絡先（`suppliers` から移す） | 電話・メール・担当者①②・携帯 |
| 頻度 | 毎月／不定期／単発 |
| 金額の性質 | 固定／変動、月額目安 |
| 勘定科目・補助科目（既定値） | 仕入高／広告宣伝費 など |
| 支払方法 | 振込／口座振替／クレジットカード／立替 |
| 支払口座 | 代表口座／さわやか信金 など |
| 支払ルール | 「月末締め・翌月16日振込」「前月25日前払い」 |
| 対象店舗 | 店舗別／全社一括 |
| 契約期間（リース等） | 開始・満了月 → 【C】の期限へ自動連携 |

これが入ると: 請求書の自動仕訳の既定値／振込タスクの自動生成（支払日の3営業日前）／**支払予定表（月次資金繰り）** が取引先マスタから作れる。

初期データは `docs/` に置く「取引先ルール一覧_トーホーエージェンシー.xlsx」（2026-09-05作成・103社）からCSV取込する。

---

## 6. 【E】従業員: 基本情報はnippo参照、給与明細は社内情報に保管

要件定義の原則（`data-ownership.md`: 人の本籍地＝nippo、給与＝社内情報の特別機密）に現状を合わせる。

| 情報 | 今 | 提案 |
|---|---|---|
| 氏名・カナ・生年月日・連絡先・住所・国籍・入社日・雇用形態・役職・所属店舗 | 社内情報とnippoの両方に手入力 | **nippoが正本**。社内情報は `users`/`employee_profiles` を参照表示のみ（ポータルアカウント紐付けは既にある）。社内情報側の入力欄は削除 |
| 給与体系（時給・固定給・賞与・交通費） | `employee_salaries` | そのまま社内情報が正本（機密） |
| **給与明細（毎月の支給実績）** | 置き場が無い | **新設 `info.payslips`**（従業員×年月: 総支給・控除内訳(jsonb)・差引支給・明細PDF）。MF給与などのCSV取込＋PDF一括アップロード。閲覧は 本人・マスター・給与権限者のみ（Storageバケット `payslips`、パスは `{employee_id}/{yyyymm}.pdf`） |
| 労働条件通知書・雇用契約書・在留カード | `documents` に店舗単位 | `documents` に **employee_id** を追加し、従業員カルテの「書類」タブから見る |
| 在留期限・資格更新 | 無し | `employees` に列追加 → 【C】の期限へ自動連携 |

- 給与計算そのものは作らない（要件定義§19）。**保管と閲覧だけ**。
- 将来: `payslips` の総支給を店舗按分して経営PLの人件費（今はDB_PL手入力＋精算書）へ自動反映できる。ただし人件費の正本をどこにするかは別途決める（§9 質問3）。

---

## 7. 【H】データ交通整理表（この情報はここに入れる）

| 情報 | 正本（入力する場所） | 参照する側（自動） |
|---|---|---|
| 法人（名称・番号・代表者・口座） | 社内情報 `info.corporations`（ハブと統一済み） | ポータル・経営D・請求書処理 |
| 店舗（番号・名称・住所・電話・店長） | ハブ `stores`（店舗番号01〜12）＋社内情報 `info.stores`（住所等の詳細） | 全システム |
| 物件契約・賃料・更新日 | 社内情報 `info.properties` | 経営PL（家賃【F】）・期限【C】 |
| 初期投資 | 社内情報 `info.store_investments`【B】 | 経営PL（減価償却・開業費） |
| ライフライン契約 | 社内情報 `info.utilities` | 取引先ルールと紐付け |
| 借入 | 社内情報 `info.loans` | 経営PL（F-8 済み）・期限【C】 |
| 取引先（連絡先・支払ルール・口座） | ポータル `public.vendors`【D】 | 社内情報・請求書処理・本部タスク・PL |
| ID/PW | 社内情報 `credentials` | — |
| 従業員の基本情報・所属・役職 | nippo `users`/`employee_profiles` | 社内情報（参照のみ）【E】 |
| 給与体系・給与明細 | 社内情報 `employee_salaries`/`payslips`【E】 | （将来）経営PL人件費 |
| 期限（許認可など元データが無いもの） | 社内情報 `info.deadlines`【C】 | ポータルホーム・Chatwork |
| 売上・PL | 経営D（`kd_*`）／BigQuery | ポータル・nippo評価 |

これを要件定義書 §16（正本の明確化）の末尾に「§16.6 社内情報の正本表」として追記する。

---

## 8. 【G】UI整理の方針

**狙い: 「今日やること・危ないもの」が最初に目に入り、それ以外は静かに並んでいる状態。**

| 項目 | 今 | 提案 |
|---|---|---|
| 色 | 青・赤・黄・緑の4色＋絵文字 | **基本1色（今の青 #1f5fbf）＋警告2色**（赤＝期限超過・要対応／黄＝30日以内）。緑は使わない（「正常」は色を付けない）。絵文字は見出しから外し、必要なら単色アイコン |
| トップ画面 | 承認待ちバナー→期限→店舗一覧→12カテゴリの縦積み | 上段「要対応」（承認待ち・期限・自分宛て）だけ色付き。中段「店舗カルテ」の一覧カード（店舗ごとに 営業許可残日数・契約更新・未入力の項目数）。下段は4グループのメニュー |
| カテゴリ | 12個が横並び | 4グループに整理: **会社**（法人・銀行・借入・不動産）／**店舗**（店舗カルテ＝物件・ライフライン・許認可・ID/PW・取引先・広告媒体）／**人**（従業員・給与明細・申請）／**横断**（書類・写真・期限・検索） |
| 一覧表 | カード羅列 | 表形式（見出し固定・並び替え・検索欄を常に上部）。スマホはカード |
| 文字の階層 | 見出しと本文の差が小さい | 見出し16px太字／ラベル12px灰／数値は右揃え・等幅 |
| 法人切替 | 上部で切替 | 維持。ただし店舗カルテでは店舗→法人が自動で決まるので切替不要にする |
| 入力画面 | 1画面に全項目 | 必須項目を上に、任意は折りたたみ。保存後は一覧に戻らずカルテに留まる |

まず「トップ画面」と「店舗カルテ」の2画面をモックで作って確認 → 他画面へ展開、の順が安全。

---

## 9. 決めていただきたいこと（実装前の質問）

1. **減価償却の扱い**: 会計（MF）の減価償却額をそのまま入れる（正確・ただし会計側の数字を取り込む仕組みが要る）か、耐用年数から簡易計算する（すぐ出せる・会計とズレる）か。→ 推奨: まず簡易計算、後で会計連携。
2. **初期投資の区分リスト**: §3.1の14区分で足りるか。減らす・足すもの。
3. **人件費の正本**: 経営PLの人件費を将来「給与明細（社内情報）」から自動で取るか、今の「DB_PL手入力＋精算書」のままにするか。
4. **給与明細の元データ**: MF給与か、他の給与ソフトか、手作成PDFか（CSVの形が決まる）。
5. **取引先の正本を `vendors` にする**ことの了承（`info.suppliers` は移行後に読み取り専用→廃止）。
6. **ポータルに出す期限の線引き**: §4.2の初期値（重要3種＋承認待ち、60日以内）でよいか。
7. **従業員基本情報の入力欄を社内情報から外す**タイミング（nippo側の項目追加（雇用形態・国籍）が先）。

---

## 10. 実施順（依存関係順）と担当の目安

| 順 | 内容 | 担当の目安 | 前提 |
|---|---|---|---|
| 1 | 【H】正本表を要件定義に追記／§9の回答 | 司令塔 | — |
| 2 | 【C】`deadlines` 強化＋自動生成＋RPC `info_alerts_for_portal()` | 担当F（社内情報） | — |
| 3 | 【C】ポータル `panel_alerts` に社内情報の期限を追加 | ポータル担当 | 2 |
| 4 | 【B】`store_investments` 新設＋店舗別合計 | 担当F | §9-1,2 |
| 5 | 【A】店舗カルテ画面（基本・物件・初期投資・ライフライン・許認可・書類） | 担当F | 4 |
| 6 | 【F】【B】家賃・減価償却を `kd_pl_monthly_summary` へ（`keiei-kd-refresh` に op 追加） | 担当A/C（経営D側） | 4, 5 |
| 7 | 【D】`vendors` 項目追加＋CSV取込＋社内情報の取引先画面差し替え | 担当C（取引先マスタ）＋担当F | §9-5 |
| 8 | 【E】従業員: nippo参照化＋`payslips`＋書類の employee_id | 担当F＋nippo担当 | §9-3,4,7 |
| 9 | 【G】UI整理（トップ→店舗カルテ→他画面） | 担当F | 5 |

守ること（既存ルール）: 既存テーブルのALTERは持ち主の担当だけが行う／本番SQLはユーザー確認後／`info` スキーマのRLSは変更前後で必ずテスト／nippoの `users` は読み取りのみ。

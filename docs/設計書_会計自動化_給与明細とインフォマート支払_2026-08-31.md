# 設計書: 会計自動化（①スマレジ給与明細→MF仕訳＋明細添付／②インフォマート発注明細→BQ・MF・振込CSV）

作成: 2026-08-31（司令塔） ／ 状態: **v1.0確定（2026-08-31 ユーザーがQ1〜Q6全回答。§5が確定仕様＝§1〜§4と食い違う場合は§5が正）**
発端: 2026-08-31ユーザー要望（項目1・項目4）

## 0. 使える資産（すでに動いているもの。ここに乗せるだけ）

| 資産 | 状態 | 本設計での使い方 |
|---|---|---|
| **マネーフォワードAPI連携**（OAuth・複数事業者=トーホー/N-Style・仕訳登録`mf-journal`・複合仕訳・**証憑添付`POST /api/v3/vouchers`**） | ✅完成・稼働中（`引継ぎ書_2026-08-27_マネーフォワード連携.md`が正本） | 給与仕訳・インフォマート請求書の仕訳・PDF添付はすべてこの基盤を呼ぶだけ |
| **スマレジ給与明細API取込**（`sf_payroll_sync`・Edge Function`smaregi-payroll-sync`・日次ワークフロー・2025-04まで遡及済み） | ✅稼働中（担当B実装） | 給与仕訳の金額データ源。支給側内訳（基本給・固定残業・通勤手当等）は取得実績あり |
| **インフォマート取込**（`ns-daily-import/tasks/infomart-siire.js`＝店舗別・日別金額のスクレイピング） | ✅稼働中 | API移行するまでの現行経路。API化後も並行確認に使う |
| 本部タスク・請求書からのMF仕訳UI（`invoices.html`・`tasks.html`） | ✅稼働中 | 「会計」まわりのUIパターンの土台 |

## 1. ①給与明細→MF仕訳＋明細添付（項目1）

### 1.1 確認結果（先に答え）
- **給与明細の中身は取れる**: `timecard.salaries:read`で支給内訳（基本給・固定残業代・通勤手当など）の取得実績あり（青山純さんの実データで検証済み）。**控除側（社会保険・所得税等）の内訳と、スマレジが発行する明細PDFそのものがAPIで取れるかは未確認**（着手時の最初の調査項目）
- **MFへの仕訳入力はできる**: `mf-journal`の`create`（複合仕訳=1本の仕訳に複数明細行）で技術的に可能
- **明細の添付もできる見込み**: 証憑API（`/api/v3/vouchers`）で仕訳にファイル添付済みの実績あり。スマレジPDFが取れない場合は、取得済みデータから明細PDFを自社生成して添付する代替案あり

### 1.2 実装案
1. 月次（給与確定後）に、`sf_payroll_sync`＋`sf_payroll_allocations`（店舗按分）から**法人×月の給与仕訳ドラフト**を組み立てる（借方: 給料手当（部門=店舗按分）／通勤費… 貸方: 未払金 or 預り金…※科目はQ2）
2. 「AI=下書き・人=承認」の既存原則どおり、**画面で内容確認→ボタンでMFへ登録**（自動サイレント登録はしない）
3. 登録成功後、**従業員ごとの給与明細（PDF）を証憑として同じ仕訳に添付**（最大件数・容量制限があるため、人数が多い月は結合PDF1本にする案が現実的）
4. 置き場所: 「会計」タブ（§3）内の「給与仕訳」メニュー

## 2. ②インフォマート（項目4）

### 2.1 やること（ユーザー要望の分解）
| # | 内容 | 難度 |
|---|---|---|
| a | **発注明細をBigQueryへ蓄積**（今は店舗別・日別合計のみ→明細粒度に） | 中（API契約が前提） |
| b | 発注実績・使用金額の分析をBQから取得できるように | 小（aの後） |
| c | **請求書PDFをMFに証憑添付**して仕訳登録 | 小（既存基盤で可） |
| d | **振込先情報→銀行の総合振込CSVを自動生成**（人間は確認してアップロード・振込実行だけ） | 中（銀行フォーマット確定が前提） |
| e | ポータルに**「会計」タブ**を新設して一元化 | 小 |

### 2.2 実装案
- **API**: インフォマート公式API（BtoBプラットフォーム）を利用（ユーザー提示のリファレンスを確認済み）。**利用には申込み・契約が必要な可能性が高い**（Q4）。契約が下りるまでは現行スクレイピング（infomart-siire.js）の拡張で明細CSVが取れないかを並行調査
- **BQ**: 新テーブル`stg_infomart_order`（発注日×店舗×取引先×品目×数量×金額）。日次でns-daily-import or GitHub Actionsから投入
- **振込CSV**: 請求データ（支払先・口座・金額・支払期日）から、指定銀行の**総合振込フォーマットCSV**を生成して「会計」タブからダウンロード。**振込の実行は絶対に自動化しない**（生成→人間が確認→ネットバンキングにアップロード→人間が承認、まで）
- **重複防止**: MF仕訳登録・振込CSVとも「どの請求書を処理済みか」をDBで管理（invoicesの`mf_journal_id`と同型）

## 3. 「会計」タブ（新設・項目4の器）

統合ポータルのサイドバーに**「🧾 会計」グループ**を新設し、以下を集約する案:
請求書メール（既存invoices）／給与仕訳（§1）／インフォマート支払い（§2: 請求書一覧→MF登録→振込CSV）／MF連携状態（事業者・トークン状態）

## 4. ユーザーへの質問

- **Q1（給与・法人）**: 給与仕訳はどの事業者（法人）のMFに入れますか？（トーホー／N-Style／両方=従業員ごとに振り分け。振り分けが必要なら、誰がどちらの法人所属かの分け方も）
- **Q2（給与・科目）**: 今、給与はMFにどう入力していますか？（過去の給与仕訳を参考にこちらで科目・形を提案→確認してもらう進め方でよいか。それとも「この形で入れて」という指定がありますか）
- **Q3（給与・明細添付）**: 添付したい「個人の給与明細」は、スマレジの明細PDFそのままが必要ですか？（APIで取れない場合、取得済みデータから同内容のPDFを自社生成して添付する形でもよいか。またMFの証憑は経理担当も見られるため、全従業員分の明細を添付してよいかの確認）
- **Q4（インフォマート）**: インフォマートAPIの利用申込み・契約はしていますか？（未契約なら申込みが最初のステップになります。申込みの窓口・条件はこちらで調べて手順書にします）
- **Q5（振込CSV）**: 振込に使う銀行とサービス名を教えてください（例: PayPay銀行の総合振込／○○銀行のビジネスダイレクト等。そのフォーマットに合わせてCSVを作ります。振込元口座が複数ならその一覧も）
- **Q6（会計タブ）**: §3の「🧾会計」グループ新設の形でよいですか？（既存の「請求書メール」もこの中に移動します）

---

## 5. 確定仕様（2026-08-31 ユーザー回答で確定。実装はここに従う）

| Q | 回答 → 確定内容 |
|---|---|
| Q1 法人 | **スマレジタイムカードは法人ごとに2アカウント（トーホー／SK）**で運用中 → 給与仕訳もアカウント=法人ごとに振り分ける。**⚠️実装時確認2点**: ①現行`sf_payroll_sync`がどちらのアカウントを取得しているか確認し、**SK側アカウントの取込を追加**（API認証情報はユーザーから受領）②MF連携済みはトーホー(default)・N-Style(nstyle)のみのため、**SKの給与仕訳を入れる場合はSK事業者のMF連携（OAuth1回・ユーザーと実施）が必要**（SKがMF会計を使っているかも合わせて確認） |
| Q2 仕訳の形 | **MFの仕訳辞書に登録済みのフォーマットで自動入力**。パターンは3つ: **社員／アルバイト／アルバイト（社会保険加入）** → 実装時にユーザーから仕訳辞書の中身（スクショで可）を受領し、パターンマスタとしてDB登録（従業員ごとにどのパターンかを判定・設定できるようにする）。以後は毎月自動でドラフト生成→確認→登録 |
| Q3 明細PDF | **APIで取れれば理想**（スマレジ画面に一括PDFダウンロードがあるため可能性あり）→ 最初にAPI取得可否を調査。取れない場合はその時に考える。**自社生成PDFのサンプルは見てみたい**とのことなので、調査と並行してサンプルを1枚作って提示する |
| Q4 インフォマート | **API申込みはまだ・有料プラン加入済みでAPI利用可能なことは確認済み** → 最初のステップ=申込み。**申込み手順書（リンク付き）を作成してユーザーに渡す**（既存ルールどおり）。申込み完了までに取込コードの設計・雛形を先行 |
| Q5 銀行 | **PayPay銀行** → PayPay銀行の総合振込用CSVフォーマット（WEB-FB・全銀形式ベース）で生成。実フォーマットはPayPay銀行の公式仕様で実装時に確定。**振込実行は自動化しない**（生成→人間が確認→アップロード→人間が承認） |
| Q6 会計タブ | **OK**＋追加要望2点を採用: ①**メールで来ない請求書のアップロード→MF処理**を会計タブ内に（本部タスク連携で実装済みの「写真アップロード→仕訳登録」をベースに独立UI化）②**会計ダッシュボード**=どの会社の・何の処理が未登録かが一目で分かるホーム画面（特にAPI連携系: メール請求書の未処理件数／インフォマート請求書の仕訳・支払状況／スマレジ給与仕訳の法人別登録状況、を法人×処理種別のマトリクスで表示） |

### 5.1 実装の割り当て（Sync5確定・2026-08-31）

| タスク | 内容 | 担当 |
|---|---|---|
| **C-7** | 🧾会計まわりの実装主担当: 会計ダッシュボード・アップロード請求書UI・**給与仕訳**（辞書パターン適用→確認画面→MF登録＋明細証憑添付）・PayPay銀行振込CSV生成・（広告設計書側の）広告費自動反映 | 担当C |
| **B-16** | スマレジAPI調査: 給与明細PDF・控除内訳・**有給残数**が取れるか＋自社生成明細PDFのサンプル作成（ユーザーに提示） | 担当B |
| **D-9** | インフォマートAPI: 申込み手順書作成（ユーザー作業）→発注明細取込→BQ`stg_infomart_order`新設→請求書PDF・振込先データをCへ受け渡し | 担当D |
| **F-10** | ポータルに「🧾会計」グループ新設（請求書メール移動・会計ダッシュボード/給与仕訳/支払いの枠。中身はCのiframe） | 担当F |
| （依頼） | SK側スマレジアカウントのAPI認証情報・仕訳辞書3パターンの中身・SKのMF連携OAuth | ユーザー（各担当から都度依頼） |

**進め方**: C-7の給与仕訳はB-16の調査完了を待たずに着手可（支給側データ=`sf_payroll_sync`は取得済み。控除内訳・PDFは調査結果が出たら組み込む）。D-9はAPI申込み待ちの間に設計・雛形まで先行。
